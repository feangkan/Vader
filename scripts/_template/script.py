#! python 3
"""
Vader script template — replace with your Rhino Python code.
Runs only through the Vader plugin (in memory).
"""
import rhinoscriptsyntax as rs

# Example: add a point at the origin
rs.AddPoint((0, 0, 0))
